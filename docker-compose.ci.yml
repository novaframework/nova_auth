services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: nova_auth_test
      POSTGRES_PASSWORD: root
    ports:
      - "5555:5432"
    tmpfs:
      - /var/lib/postgresql/data
